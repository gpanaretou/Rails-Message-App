    <nav class="navbar navbar-expand-lg bg-dark navbar-dark text-bg-dark">
    <div class="container">

        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <%= link_to 'Chatroom', root_path, class:"nav-link" %>
                <a class="nav-link" href="#">Messages</a>
            </div>
                
                
            <div class="navbar-nav ms-auto">
                
                <% if logged_in? %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <%= current_user.username %>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-dark">
                            <%= link_to 'Log out', logout_path, class:"dropdown-item", data: {turbo_method: :delete} %>
                        </ul>
                    </li>
                <% else %>
                    <a class="nav-link" href="#">Messages</a>
                    <%= link_to 'Log in', login_path, class:"nav-link" %>
                    <%= link_to 'Sign up', root_path, class:"nav-link btn btn-outline-success me-2" %>
                <% end %>

            </div>
        </div>
    </div>
    </nav>